# Diff between v1 and v2

The evaluation criterion was initially wrong, and the lecturer has provided a new zip file which we use.
The only stuff which is different is the latter section of the `Machine_learning_task_for_TDT4173.pdf` file, in addition to the fact that 0.8 has been replaced with 0.2 in four different spots in the `kaggle_metric.ipynb` file.
